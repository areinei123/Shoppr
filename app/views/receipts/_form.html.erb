<%= form_for [@group, @grocery_list, @receipt] do |f| %>
  <%= f.file_field :photo, id: "receipt_field" %>
  <%= f.submit "Upload New Receipt", class: "button postfix" %>
<% end %>

<%= form_for [@group, @grocery_list, @receipt] do |f| %>
  <%= f.label :photo, 'Receipt URL' %>
  <%=f.text_field :photo %>
  <%= f.submit 'Add receipt' %>
<%-end-%>

